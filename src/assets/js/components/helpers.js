export function numberToDay(j) {
    return ('0' + j).slice(-2);
}


// show WIDTH or HEIGHT on console
window.addEventListener('resize' , () => {
    // SHOW WIDTH
    // console.log(`${window.innerWidth} `);

    // SHOW HEIGHT
    // console.log(`La hauteur de l'écran est ${window.innerHeight}`);
})


